#include<iostream>
using namespace std;
class Product
{
	private:
	string name;
	string stockItems;
	string price;
	private:
		void setprice(string);
		string getprice();
		discount=0;
		if (number_to_be_bought < 10)
		pass
		else	10 <= number_to_be_bought < 99:
            discount = 10
        else:
            discount = 20
        price = (100 - discount) / 100 * self.price
        return price * number_to_be_bought
            
			
}
